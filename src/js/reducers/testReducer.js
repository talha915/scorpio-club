import Data from '../../data/Data';

const initial = [...Data];

export default function (state = initial) {
  /* switch(action.type) {
    case FILL_ME:
      return {...state}
  } */
  return state;
}
